See if Gear can autopopulate id from the JSON.
Build the rest of the model
See if we can append to existing JSON
Pretty-print the JSON output(s)
Clean up the label / input boxes in the HTML
and toss it all in panels